{{define "content"}}
<div class="max-w-md bg-white p-6 rounded-2xl border space-y-3">
  <h1 class="text-2xl font-semibold">Canceled</h1>
  <p class="text-gray-700">Registration <span class="font-mono">{{.Code}}</span> has been canceled.</p>
  <a class="text-sm underline" href="/">Back to home</a>
</div>
{{end}}
{{define "parents/cancel_done.tmpl"}}{{template "base" .}}{{end}}