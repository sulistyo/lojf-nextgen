{{define "content"}}
<h1 class="text-2xl font-bold mb-1">Start Registration</h1>
{{if .Parent}}
<p class="text-sm text-gray-600 mb-3">
  Hi, {{.Parent.Name}} <span class="font-mono">({{.Parent.Phone}})</span> —
  <a class="underline" href="/account/logout?next=/register">not you? use a different number</a>
</p>
{{end}}
<form method="POST" action="/register" class="grid gap-4 max-w-lg bg-white p-6 rounded-2xl border">
  <div>
    <label class="block text-sm mb-1">Parent Phone</label>
    <input name="phone" class="w-full rounded-xl border p-2" value="{{.Phone}}" required>
  </div>
  <button class="px-4 py-2 rounded-xl bg-gray-900 text-white">Continue</button>
</form>
{{end}}
{{define "parents/register_phone.tmpl"}}{{template "base" .}}{{end}}
