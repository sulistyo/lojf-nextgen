{{define "content"}}
<div class="max-w-md bg-white p-6 rounded-2xl border space-y-3">
  <h1 class="text-2xl font-semibold">Checked-in</h1>
  <p class="text-gray-700">Code <span class="font-mono">{{.Code}}</span> has been marked as checked-in.</p>
  <a class="text-sm underline" href="/checkin">Back to Check-in</a>
</div>
{{end}}
{{define "admin/checkin_done.tmpl"}}{{template "base" .}}{{end}}